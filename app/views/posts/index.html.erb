<h1>posts page</h1>

<%= link_to "Create New Post", posts_new_path %>

<%= @posts.each do |post| %>
  <p>
    [<%= post.id %>] <%= post.title %>
  </p>
  <p>
    <%= post.body %>
  </p>


  <%= link_to "Show", post %>
  <%= render 'posts/shared', post: post%>

<% end %>